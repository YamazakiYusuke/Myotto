<h1 id="top_title"><%= t('.new_post') %></h1>
<div class='index_container'>
  <% @translations.each do |translation| %>
    <div class="col-xs-12 col-sm-offset-2 col-sm-8 col-md-offset-3 col-md-6 index_post">
      <span><%= t('.contributor') %>: <strong><%= link_to(translation.user.name, user_path(translation.user.id)) %></strong></span><br/> 
      <span><%= t('.original') %>: <%= translation.sentence.content %></span><br/> 
      <span><%= t('.translation') %>: <%= translation.content %></span><br/> 
      <span><%= translation.user_translation_favorites.size %>Like</span>
      <span><%= link_to t('.show'), translation_path(translation.id) %></span>
    </div>  
    <br/><br/>
  <% end %>
  <div id="under"></div>
</div>
<br>
<div id="index_pagenate"><%= paginate @translations %></div>
